# BITO: DURAK ONLINE (ДИПЛОМНЫЙ ПРОЕКТ)
## КОНСТИТУЦИЯ И ПРАВИЛА ПРОЕКТА ДЛЯ AI-АССИСТЕНТА (GEMINI.md)

---

### 1. О ПРОЕКТЕ И ЦЕЛИ (WHY & WHAT)
- **Суть проекта:** Мультиплеерная карточная игра «Дурак Онлайн» (подкидной и переводной) в реальном времени с рейтинговой системой ELO, игровой экономикой (виртуальные фишки, банк, комиссия), системой лобби, авторизацией, чатом и списком друзей.
- **Главная цель:** Дипломный проект по окончании 3-летнего обучения, рассчитанный на максимальную оценку комиссии при защите, а также практическое освоение архитектуры высоконагруженных сетевых Go-сервисов реального времени.
- **Приоритет качества:** Чистая, объяснимая и расширяемая архитектура (Clean Architecture), идиоматичный Go, 100% покрытие unit-тестами игровой логики, надёжность и безопасность.

---

### 2. РОЛЬ АССИСТЕНТА И СТИЛЬ МЕНТОРСТВА (HOW WE WORK)
- **Роль:** Senior Go Backend Architect, Lead Mentor и парный разработчик.
- **Формат объяснений:**
  - Не просто выдавать готовый код, а **объяснять архитектурные решения, паттерны и «почему сделано именно так»**.
  - Разбирать сложные концепции: конкурентность в Go (горутины, каналы, мьютексы, контексты, data race detection), стейт-машины, вебсокеты, транзакции в БД, чтобы автор проекта мог уверенно ответить на любой вопрос экзаменационной комиссии.
- **Инструменты ассистента (MCP & Tooling):**
  - Использовать `gopls-mcp-server` для статического анализа Go, проверки типов и рефакторинга.
  - Обязательно проверять код на гонки: `go test -race ./...`.
- **Языки:**
  - Общение и комментарии к архитектуре: русский язык.
  - Код, переменные, структуры, логирование, документация в коде: английский язык.

---

### 3. ТЕХНОЛОГИЧЕСКИЙ СТЕК (TECH STACK)

#### Backend:
- **Язык:** Go (версия 1.27+).
- **Сетевой транспорт:** WebSockets (`coder/websocket` или `gorilla/websocket`) + REST HTTP (для авторизации/профилей).
- **Архитектура ядра (In-Memory Engine):**
  - Игровые комнаты и состояние активных матчей работают **в памяти Go-процесса** с использованием горутин, каналов и потокобезопасности (`sync.RWMutex`).
  - Каждый активный стол изолирован, сообщения обрабатываются последовательно через событийную очередь комнаты (Event Loop / Actor-like паттерн).
- **База данных & Хранилище:**
  - **PostgreSQL 16:** постоянные данные (пользователи, история матчей, транзакции баланса, друзья, настройки).
  - **Драйвер и SQL:** чистый типизированный SQL с использованием `pgx` (v5) / `sqlc` (без тяжелых медленных ORM).
  - **Миграции:** `golang-migrate` или `goose`.
  - **Redis 7:** сессии, кэш онлайн-статусов пользователей, временные блокировки.
- **Почта/Коды:** SMTP сервис (локально Mailpit в Docker) для отправки кодов подтверждения email.

#### Frontend:
- **Фреймворк:** Vue 3 (Composition API) + TypeScript.
- **Стейт-менеджмент:** Pinia.
- **Сборщик и стили:** Vite + Tailwind CSS.
- **UI & Анимации:** Адаптивный веб (Desktop + Mobile), плавная отрисовка карт и стола.

#### Инфраструктура:
- **Docker & Docker Compose:** локальный запуск PostgreSQL, Redis, Mailpit.

---

### 4. ПРАВИЛА БЕЗОПАСНОСТИ И АРХИТЕКТУРНЫЕ ПРИНЦИПЫ

1. **Server-Authoritative (Нулевое доверие клиенту):**
   - Клиент никогда не управляет состоянием игры и не имеет доступа к картам колоды или рукам оппонентов.
   - Сервер валидирует каждый шаг: «Имеет ли право игрок сейчас ходить?», «Есть ли эта карта в его руке?», «Бьет ли эта карта карту на столе?», «Не превышен ли лимит подкидывания?».
   - Транзакции фишек (ставки, выигрыш, комиссия) выполняются атомарно в PostgreSQL с защитой от race condition (`SELECT ... FOR UPDATE` или транзакции уровня изоляции `Repeatable Read`).
2. **Graceful Disconnect (Обработка обрывов связи):**
   - При дисконнекте игрока запускается таймер **Grace Period (30 секунд)**.
   - Если игрок вернулся — стейт стола синхронизируется в полном объеме.
   - Если время истекло — фиксируется техническое поражение (Fold), банк распределяется, ставка списывается.

---

### 5. ТОЧНАЯ ФУНКЦИОНАЛЬНАЯ СПЕЦИФИКАЦИЯ (REQUIREMENTS & RULES)

#### 5.1 Авторизация и аккаунты
- **Гостевой вход (Guest Login):** создание временного аккаунта в 1 клик со стартовым балансом для моментального тестирования и демонстрации комиссии в двух окнах браузера.
- **Полноценная регистрация:** Email с кодом подтверждения (Mailpit) + пароль (JWT токены access/refresh).
- **OAuth (расширение):** Google, VK, Telegram.
- **Профиль:** аватар, никнейм, баланс монет, ELO рейтинг, статистика (игры, победы, поражения, винрейт).

#### 5.2 Меню и Лобби
- **Быстрая игра:** авто-поиск стола 1х1 по диапазону ставок игрока (500, 1000, 2500, 5000, 10000).
- **Список столов (Лобби):** фильтрация, просмотр активных столов.
- **Создание стола:**
  - Тип: Подкидной / Переводной.
  - Ставка: 500, 1000, 2500, 5000, 10000.
  - Колода: 36 карт (на старте), расширение до 24 и 52 карт.
  - Игроки: 1х1 (на старте), расширение до 2-6 игроков и командного режима 2х2.
  - Приватность: публичный или с паролем.
- **Мировой чат:** real-time чат со смайлами и стикерами.
- **Система друзей:** статус (онлайн, в игре, оффлайн), вызов на дуэль, личные сообщения, перевод монет.

#### 5.3 Игровой процесс (Core Rules & State Machine)
- **Таймер хода:** **20 секунд** на принятие решения.
- **Определение первого хода:** игрок с наименьшим козырем на руках.
- **Правило первого кона:** первый отбой (бита) — **максимум 5 карт** (вместо обычных 6).
- **Правило перевода:** переводить можно картой того же достоинства (нельзя перевести, если у следующего игрока в руке меньше карт, чем на столе).
- **Лимит подкидывания:** не более 6 карт за раунд и не более, чем карт в руке у отбивающегося.
- **Фазы раунда:** «Бита» (в отбой) или «Взял» (в руку защищающегося).
- **Добор карт:** по очереди из колоды (атакующий -> подкидывающие -> защищавшийся) до 6 карт.
- **Ничья (Draw):** возможна, если оба игрока одновременно сбросили последние карты в отбой. При ничьей ставки возвращаются обоим игрокам в полном объеме (без списания комиссии Rake), ELO не меняется или меняется нейтрально.

#### 5.4 Экономика и Рейтинг
- **Стартовый капитал:** **2 500 фишек** каждому новому игроку (включая гостя).
- **Бесплатные фишки (Free Coins):** **500 фишек каждые 4 часа**, если текущий баланс игрока меньше 500.
- **Банк и Rake:**
  - При победе: $\text{Выигрыш} = \text{Банк} - 5\% \text{ Rake}$.
  - Комиссия 5% удерживается сервером и выводится из оборота.
  - При ничьей: возврат $100\%$ ставки обоим игрокам (0% комиссии).
- **Рейтинг ELO:**
  - Базовый рейтинг: $1000$ ELO.
  - Классическая формула ELO с динамическим $K$-фактором ($K=32$ для первых 30 игр, $K=16$ для опытных).

---

### 6. СТРУКТУРА КАТАЛОГОВ ПРОЕКТА (GO STANDARD)
```
bito/
├── GEMINI.md              # Конституция и правила проекта
├── README.md              # Документация для GitHub
├── cmd/
│   └── server/            # Точка входа бэкенда (main.go)
├── internal/
│   ├── config/            # Загрузка env/конфигурации
│   ├── game/              # Изолированное ядро игры (карты, правила, стейт-машина)
│   │   ├── card.go
│   │   ├── deck.go
│   │   ├── table.go
│   │   ├── rules.go
│   │   └── durak_test.go  # 100% unit-тесты
│   ├── lobby/             # Менеджер комнат, подбор игроков (matchmaking)
│   ├── transport/
│   │   ├── http/          # REST API (auth, profile, stats)
│   │   └── ws/            # WebSocket хаб, клиентские сессии, диспетчер событий
│   ├── service/           # Бизнес-логика (пользователи, кошелек, ELO расчет)
│   ├── repository/        # Доступ к PostgreSQL (pgx/sqlc)
│   └── models/            # Доменные структуры данных
├── migrations/            # SQL-миграции базы данных
├── web/                   # Frontend приложение на Vue 3 + TypeScript + Pinia
├── docker-compose.yml     # Инфраструктура (Postgres, Redis, Mailpit)
├── Makefile               # Команды сборки, тестов и запуска
├── go.mod
└── go.sum
```
